# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'AKboard' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AKboard

end

target 'MyKeyboard' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyKeyboard
  pod 'Eureka'

  target 'MyKeyboardTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyKeyboardUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
